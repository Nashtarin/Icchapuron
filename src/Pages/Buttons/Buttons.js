import React from 'react';
import pic from '../../images/dashboard.png'

const Buttons = () => {
    return (
        <div>
        <img src={pic} alt="" />
        </div>
    );
};

export default Buttons;