import React from 'react';
import pic from '../../images/lockscreen.png'

const LockScreen = () => {
    return (
        <div>
            <img src={pic} alt="" />
        </div>
    );
};

export default LockScreen;